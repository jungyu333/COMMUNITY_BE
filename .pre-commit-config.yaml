default_language_version:
    python: python3.11

exclude: |
    ^\.pre-commit-config\.yaml$

repos:
-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
    -   id: check-added-large-files
    -   id: check-toml
    -   id: check-yaml
        args:
        -   --unsafe
    -   id: end-of-file-fixer
    -   id: trailing-whitespace
-   repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.1.2
    hooks:
    -   id: ruff
        exclude: migrations/
        args:
        - --fix
    -   id: ruff-format
-   repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.8.0
    hooks:
      - id: mypy
        exclude: migrations/
        additional_dependencies:
            [
                "redis==4.5.1",
                "types-redis==4.6.0",
                "types-ujson==5.9.0",
            ]
-   repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        name: isort (python)

-   repo: local
    hooks:
    -   id: auto-commit
        name: Auto commit pre-commit changes
        entry: python -c "import os, subprocess; shell = 'cmd /c' if os.name == 'nt' else 'bash -c'; commit_status = subprocess.run(shell + ' \"git add . && git commit --amend --no-edit --no-verify\"', shell=True).returncode; exit(1 if commit_status == 0 else 0)"
        language: system
        pass_filenames: false

ci:
    autofix_commit_msg: 🎨 [pre-commit.ci] Auto format from pre-commit.com hooks
    autoupdate_commit_msg: ⬆ [pre-commit.ci] pre-commit autoupdate
